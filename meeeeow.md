29) Схема разделения секрета по Шамиру — это (k, n)-пороговая криптосистема, где секрет делится на n частей так, что:
- любые k частей могут восстановить секрет,
- менее k частей не дают никакой информации о нём.         

Идея: секрет S — это значение полинома в точке 0.
Создаётся случайный полином степени k−1:
𝑓(𝑥) = 𝑆 + 𝑎1x + 𝑎2𝑥 + ⋯ + 𝑎𝑘−1 * 𝑥^𝑘−1

Каждому участнику выдают долю (𝑥𝑖,𝑓(𝑥𝑖))
Когда собираются k участников, они интерполируют полином (обычно методом Лагранжа) и находят:          
S=f(0)

Менее чем k точек недостаточно, чтобы однозначно восстановить полином — поэтому секрет остаётся защищённым.

23) *Метод Касиски* — это классический криптоаналитический метод взлома шифра Виженера, основанный на поиске повторяющихся последовательностей букв в шифртексте.
Кратко суть метода:
- Находим повторяющиеся фрагменты (обычно 3 буквы и более) в зашифрованном тексте.
- Измеряем расстояния между повторами — число символов между одинаковыми последовательностями.
- Вычисляем общие делители этих расстояний.
- Самый вероятный общий делитель — это длина ключевого слова.
- Зная длину ключа, текст разбивается на подстроки, каждая из которых зашифрована одним сдвигом Цезаря.
Далее применяется частотный анализ, чтобы определить сдвиги и восстановить ключ.

Идея:
Повторяющиеся фрагменты возникают тогда, когда фрагменты открытого текста попадают под одинаковые позиции ключа — каждые k символов. Поэтому расстояния между такими совпадениями часто кратны длине ключа.


Задача:         
1) Выбор простых:
p = 101 (простое)       
q = 113 (простое)         
p,q — два разных простых числа, секретно выбираются для генерации ключей RSA

2) Вычисляем модули:         
n=p⋅q=101⋅113=11413           
n — модуль (часть публичного и приватного ключа). Все операции шифрования/расшифровки выполняются по модулю

φ(n)=(p−1)(q−1)=100⋅112=11200.
φ(n) (функция Эйлера) — количество целых чисел 
1 ≤ x ≤ n−1, взаимно простых с n. В RSA используется для поиска обратного элемента 𝑑

3) Выбор публичной экспоненты 𝑒
Выбираем e такое, что 1<e<φ(n) и gcd(e,φ(n))=1.
Взяли 𝑒 = 17

Пояснение: 
gcd(e,φ(n))=1 означает, что φ(n) взаимно просты — это необходимо, чтобы у e существовал обратный по модулю φ(n).
